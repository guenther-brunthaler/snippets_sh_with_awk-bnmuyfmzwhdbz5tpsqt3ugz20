$APPLICATION_NAME - sufficiently efficient modular addition

Calculate (<augend> + <addend1> + ...) modulo <modulus>
with sufficient efficiency even for large numbers, such as those
required for various cryptographic primitives.

Usage: $APPLICATION_NAME <augend> [ <addend1> ... ] <modulus>
